@using VolunteerHub.Mvc.Services
@model List<TotalByVolunteerRow>

<h2>Report: Totals by Volunteer</h2>
<p class="description">This report groups help records by volunteer and calculates total amount.</p>

<form method="get">
    From: <input type="date" name="from" value="@ViewBag.From" />
    To: <input type="date" name="to" value="@ViewBag.To" />
    <button type="submit">Apply</button>
    <a asp-action="TotalsByVolunteer">Reset</a>
</form>

<br />

<table>
    <thead>
        <tr>
            <th>Volunteer</th>
            <th>Records</th>
            <th>Total amount</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var row in Model)
        {
            <tr>
                <td>@row.VolunteerName</td>
                <td>@row.RecordsCount</td>
                <td><b>@row.TotalAmount.ToString("N2") UAH</b></td>
            </tr>
        }
    </tbody>
</table>
